<div class="col-lg-4 text-center">

    <img 
        src="{{ asset($imagen) }}" 
        alt="{{ $nombre }}"
        class="card-img"
        width="200"
        height="200"
    >

    <h4>{{ $nombre }}</h4>

    <p>$ {{ $precio }}</p>

</div>