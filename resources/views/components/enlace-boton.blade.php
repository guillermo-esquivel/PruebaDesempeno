<a href="{{ route($route) }}" class="text-decoration-none text-center d-block">

    <div class="circulo-boton">
        <img src="{{ asset($image) }}" alt="{{ $title }}">
    </div>

    <h3 class="mt-3 text-dark">{{ $title }}</h3>

</a>